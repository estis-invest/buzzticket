services:
  db:
    image: postgres:15-alpine
    container_name: buzzticket-db
    environment:
      POSTGRES_DB: buzzticket
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"